# generated_by: {{ tag.generated_by }}
# spec_id: {{ tag.spec_id }}
# spec_version: {{ tag.spec_version }}
# DO NOT EDIT — rendered from the approved spec; changes belong in the spec.
# Include from the consuming bundle's databricks.yml.
resources:
  jobs:
    {{ spec.entity }}_recon:
      name: "[{{ spec.entity }}] reconciliation (spec {{ tag.spec_id }} v{{ tag.spec_version }})"
      tags:
        generated_by: {{ tag.generated_by }}
        spec_id: {{ tag.spec_id }}
        spec_version: "{{ tag.spec_version }}"
      tasks:
        - task_key: recon
          spark_python_task:
            python_file: ./recon_job.py
          environment_key: default
      environments:
        - environment_key: default
          spec:
            client: "2"
